import SiteHeader from './SiteHeader';
import CampaignHeader from './CampaignHeader';

const Header = () => {  
  return (
    <header>
      <SiteHeader />
      <CampaignHeader />
    </header>
  );
};

export default Header;
