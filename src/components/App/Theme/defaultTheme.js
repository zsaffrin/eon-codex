const space = {
  thin: '2px',
  sm: '0.25rem',
  md: '0.5rem',
  lg: '1rem',
  xl: '2rem',
};

const layout = {
  borderRadius: space.md,
  padding: space.lg,
};

const defaultTheme = {
  space,
  layout,
};

export default defaultTheme;
